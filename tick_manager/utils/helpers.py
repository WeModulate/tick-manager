def format_result(result: float) -> str:
    """
    Format the result to two decimal places.

    Args:
        result (float): The result to format.

    Returns:
        str: The formatted result as a string with two decimal places.
    """
    return f"{result:.2f}"
